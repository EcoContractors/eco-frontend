# Backend API URL (server-side only - do not prefix with VITE_)
API_BASE_URL=http://localhost:5000

# Public environment variables (accessible in browser - prefix with PUBLIC_)
# PUBLIC_APP_NAME=EcoContractors
